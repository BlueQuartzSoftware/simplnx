# V&V Report: ErodeDilateBadDataFilter

|                             |                                                                          |
|-----------------------------|--------------------------------------------------------------------------|
| Plugin                      | SimplnxCore                                                              |
| SIMPLNX UUID                | `7f2f7378-580e-4337-8c04-a29e7883db0b`                                   |
| SIMPLNX Human Name          | Erode/Dilate Bad Data                                                    |
| DREAM3D 6.5.171 equivalent  | `ErodeDilateBadData` — SIMPL UUID `3adfe077-c3c9-5cd0-ad74-cf5f8ff3d254` |
| Verified commit             | *<filled at SBIR deliverable assembly>*                                  |
| Status                      | READY FOR REVIEW                                                         |
| Sign-off                    | Delegated to the PR reviewer (requester decision 2026-08-19)              |

## At a glance

| Aspect                 | Current state |
|------------------------|----------------|
| Algorithm Relationship | **Port** — legacy `ErodeDilateBadData.{h,cpp}` was diffed line-by-line against `Algorithms/ErodeDilateBadData.cpp` this pass. Neighbor offsets, boundary-validity checks, vote/tie-break order, and transfer conditions are structurally identical. One divergence found and fixed — see Bug Fixes. |
| Oracle (confirmed)     | **Class 2 (Reference implementation).** The 28 expected `FeatureIds`/`Misc` arrays (7 direction combinations × 2 operations × 2 iteration counts) are genuine DREAM3D 6.5.171 output, matched element-wise against SIMPLNX and compiled into `ErodeDilateBadDataTest.cpp` as constants so the comparison re-runs in CI without the legacy binary. Confirmed — `(Erode) Expanded` and `(Dilate) Expanded` pass, 28/28 combinations. |
| Code paths enumerated  | **13 of 15 exercised.** All 6 face directions (-Z/-Y/-X/+X/+Y/+Z) confirmed hit by instrumentation. Paths 12 and 13 (mixed-type/multi-component `copyTuple`, >1 concurrent transfer task) were forfeited with the archive-test retirement on 2026-08-18 and **restored at toy scale on 2026-08-19** by the inline `(Multi-Type Transfer)` fixture. Path 15 (the `NumIterations < 1` preflight guard) was **added 2026-08-19** and is covered from the outset. Remaining: the `m_ShouldCancel` early-exit (Path 11), never covered — the sole genuine gap; and Path 14, **reclassified** — source review of `GenerateDataArrayList` shows no such filtering branch exists, so it is now guarded by assertion rather than exercised (see row 14). |
| Tests today            | **8 TEST_CASEs, all pass** — in-core measured 2026-08-19 at 2589 assertions; the OOC result is the pre-removal run and its re-measurement is **waived** (requester decision 2026-08-19, see Test inventory): 2 `GENERATE` parameter sweeps (14 valid runs each over direction × iteration count) + 1 multi-type transfer test + 1 ignored-path test + 3 preflight-error tests + 1 SIMPL backwards-compat. The production-scale exemplar-archive comparison (`(Erode)`, 55 assertions) was retired 2026-08-18 and stays retired; the code-path coverage it alone carried is back inline — see dated notes below. |
| Exemplar archive       | `6_6_erode_dilate_test.tar.gz` — provides `Input Data` plus legacy-generated `Exemplar Bad Data Erode` / `Exemplar Bad Data Dilate` containers on a 189×201×20 Small IN100 slice. Formerly consumed by the `(Erode)` test here (removed 2026-08-18, see dated note below); still shared with `ErodeDilateMaskTest` and `ErodeDilateCoordinationNumberTest`. SHA512 verified against `test/CMakeLists.txt`. |
| Legacy comparison      | **Run.** Two independent comparisons: (1) all 28 parameter combinations run through DREAM3D 6.5.171 `PipelineRunner` against an HDF5 twin of the inline fixture and diffed element-wise — **28/28 exact matches** on both `FeatureIds` and `Misc`; (2) the now-removed `(Erode)` test compared SIMPLNX against a legacy-generated exemplar at production scale (759,780 cells, 6 cell arrays) — passed prior to its 2026-08-18 removal; see dated note below. |
| Bug flags              | `ErodeDilateBadDataFilter-D1` (X/Y/Z direction parameters had no effect) — **confirmed and fixed this pass.** One additional hypothesis (Dilate tie-break order) was investigated, found to be a false lead, and reverted — see deviations doc. A second port omission — legacy's non-positive `NumIterations` guard was dropped, so SIMPLNX accepted the value and silently no-opped — was **restored 2026-08-19** (requester decision, applied family-wide) and filed as `ErodeDilateBadDataFilter-D2`. |
| V&V phase              | Oracle chosen and confirmed; legacy comparison run; direction-masking bug fixed; zero-dimensions preflight path now covered; Paths 12–13 restored inline 2026-08-19. Outstanding: formalizing the manual 28-combination A/B run as an automated archive-based test — **declined 2026-08-18, risk accepted** (not resolved). The archive-pinned `(Erode)` test was retired the same day, which does not address this item and mildly worsens it: it removed the only in-repo oracle independent of the inline constants. The A/B evidence remains manually regenerated. Outstanding before promotion to COMPLETE: sign-off — **delegated to the PR reviewer** (requester decision 2026-08-19, consistent with the program plan's standing practice); and the uncovered cancel path (Path 11). The OOC re-run since the retirement is **waived** (requester decision 2026-08-19). The Paths 12–14 gap accepted 2026-08-18 is closed for 12–13 and reclassified for 14. |

> **2026-08-18 update:** the archive-pinned `SimplnxCore::ErodeDilateBadDataFilter(Erode)` exemplar test (the production-scale comparison against `6_6_erode_dilate_test.tar.gz`) has been removed. The 28-combination inline Class 2 oracle from PR #1687 (`(Erode) Expanded` / `(Dilate) Expanded`) is legacy-binary-verified across every operation × direction × iteration combination and supersedes the single-combination 6.6-derived exemplar diff **in parameter space** (28 combinations vs 1). It does not supersede it in every respect: the mixed-type/multi-component `copyTuple` dispatch, the concurrent multi-task transfer, and the heterogeneous-AttributeMatrix coverage the archive test carried were **forfeited on that date** — recorded then as an accepted, documented gap, not a wash. See the *Coverage split* paragraph and rows 12–14 of the code-path table.
>
> **2026-08-19 update — Paths 12 and 13 restored at toy scale.** A new inline `SimplnxCore::ErodeDilateBadDataFilter(Multi-Type Transfer)` TEST_CASE re-covers the *code-path* half of what 2026-08-18 forfeited, without any archive dependency. It reuses the 4×4×2 fixture and adds four more cell arrays of distinct element types and component counts (`Float32x3`, `UInt8x1`, `Float64x2`, `Int64x1`) alongside the existing `Misc` `int32`, so the transfer stage dispatches `copyTuple` over five type/component shapes (**Path 12**) from five simultaneously enqueued `ErodeDilateBadDataTransferDataImpl` tasks (**Path 13**). **Path 14 is reclassified rather than restored** — see row 14 of the code-path table for the `GenerateDataArrayList` source finding that motivated it. What is **not** restored is production scale: 32 cells vs 759,780. That half of the retirement stands, deliberately. The `6_6_erode_dilate_test.tar.gz` archive itself is **not yet retired** — `ErodeDilateMaskTest` and `ErodeDilateCoordinationNumberTest` still consume it — and will be retired once all three consumer conversions have merged.

## Summary

`ErodeDilateBadDataFilter` erodes or dilates voxels with `FeatureId == 0` ("bad data") in an `ImageGeometry`, optionally restricted to any non-empty combination of X, Y, and Z face directions. Verification is **Class 2**: the 28 expected output arrays compiled into `(Erode) Expanded` / `(Dilate) Expanded` are genuine DREAM3D 6.5.171 output for the same fixture, matched element-wise across every operation × direction × iteration combination; the production-scale `(Erode)` archive comparison was retired 2026-08-18 as superseded *in parameter space* by that 28-combination oracle, at the cost of the mixed-type/multi-component `copyTuple`, concurrent-transfer, and heterogeneous-AM coverage it alone carried; the first two of those were restored inline on 2026-08-19 by `(Multi-Type Transfer)` at toy scale, the third was reclassified, and production scale remains retired (see the dated notes and *Coverage split* above). One SIMPLNX-side bug was found and fixed this pass (`ErodeDilateBadDataFilter-D1` — the direction parameters had no effect at all), and one further port omission was closed on 2026-08-19: legacy's non-positive `NumIterations` preflight guard (6.5.171 error `-5555`) had been dropped in the port, so SIMPLNX accepted `0` or a negative value and silently no-opped; it is restored as `-14603` under a requester decision applying the same validation across the whole erode/dilate family. All 8 tests pass in-core at 2589 assertions (measured 2026-08-19), with the OOC re-measurement waived by the requester on 2026-08-19.

## Algorithm Relationship

*Classification:* **Port** ~~| Minor changes | Rewrite | New filter~~

*Evidence:* `SimplnxCoreLegacyUUIDMapping.hpp` maps legacy SIMPL UUID `3adfe077-c3c9-5cd0-ad74-cf5f8ff3d254` directly to `FilterTraits<ErodeDilateBadDataFilter>`, and `test/simpl_conversion/{6_4,6_5}/ErodeDilateBadDataFilter.json` carry the legacy `Direction`/`NumIterations`/`XDirOn`/`YDirOn`/`ZDirOn`/`FeatureIdsArrayPath`/`IgnoredDataArrayPaths` parameter set unchanged — the same filter with the same parameter model, not a reimplementation. Legacy source (`Source/Plugins/Processing/ProcessingFilters/ErodeDilateBadData.{h,cpp}`, from a sibling `DREAM3D` checkout on the authoring engineer's machine, not committed to this repository) was diffed line-by-line against `Algorithms/ErodeDilateBadData.cpp` this pass rather than inferred from documentation.

*Port-time deltas:*

1. **Face-neighbor offsets** — legacy computed `neighpoints[]` inline; SIMPLNX calls `initializeFaceNeighborOffsets(dims)` from `NeighborUtilities`. Same six offsets in the same `[-Z,-Y,-X,+X,+Y,+Z]` order; no output change.
2. **Boundary-validity checks** — legacy tested each face boundary with inline conditionals; SIMPLNX calls `computeValidFaceNeighbors(x, y, z, dims)`. Reproduces the same six conditions; no output change.
3. **Direction gating** — legacy ORs the direction flag into each per-face boundary check (`|| !m_ZDirOn`); SIMPLNX masks the per-voxel `isValidFaceNeighbor` array in `adjustValidNeighbors`. Equivalent once wired in — but it was *not* wired in, which is `ErodeDilateBadDataFilter-D1`. Now fixed and legacy-verified.
4. **Parallel array transfer** — legacy transferred all cell arrays in one serial pass interleaved with `FeatureIds`; SIMPLNX uses `ParallelTaskAlgorithm` for the non-`FeatureIds` arrays and transfers `FeatureIds` afterward, serially. Proven equivalent: erode only maps 0→>0 and dilate only >0→0, `neighbors[]` always points at a voxel whose relevant polarity is preserved, and each index is written at most once per pass, so no transfer predicate can observe a changed value. No output change.
5. **Cancel check** — SIMPLNX reads `m_ShouldCancel` once per Z-slice; legacy has no cancel check at all. Additive; no output change on a run to completion.
6. **Progress reporting** — SIMPLNX reports per-array progress through `MessageHelper`/`ThrottledMessenger`; legacy used `notifyStatusMessage`. No output change.

*Material PRs since baseline:* four PRs have touched `Algorithms/ErodeDilateBadData.cpp` and account for the port-time deltas above:

- **#1523** — factored the 6-face-neighbor code out into `NeighborUtilities` (deltas 1–2, and the `isValidFaceNeighbor` array that delta 3 masks).
- **#1590** — standardized 2D image handling (`VoxelNeighbors<Image3D>` specialization).
- **#1340** — thread-safe messaging rework (delta 6).
- **#1687** — this branch: fixes D1, adds the `-14601`/`-14602` preflight guards, and rebuilds the test suite.

Earlier commits (#1249, #1017, #1013, #801) are compiler-warning, store-API, and rename churn with no behavioral content.

*SIMPLNX implementation:* `Algorithms/ErodeDilateBadData.cpp` (231 lines) uses `NeighborUtilities::VoxelNeighbors<Image3D>` for face-neighbor offsets and boundary validity, and `ParallelTaskAlgorithm` to transfer non-`FeatureIds` arrays in parallel.

## Bug Fixes (this pass)

### ErodeDilateBadDataFilter-D1: Direction parameters had no effect — fixed

See deviations doc for full detail. Summary: `adjustValidNeighbors` was dead code (defined, never called); `XDirOn`/`YDirOn`/`ZDirOn` had zero effect on which face neighbors participated. Fixed by retyping the helper to mask the actual per-voxel validity array (`isValidFaceNeighbor`) with correct axis mapping, and calling it at `Algorithms/ErodeDilateBadData.cpp:162-163` for every bad-data voxel. Verified by regenerating all 28 exemplar constants from real DREAM3D 6.5.171 output (they were previously byte-identical across all 7 direction combinations for a given operation/iteration count) and matching them element-wise against SIMPLNX for every combination — see Oracle section.

### Port omission: legacy's non-positive `NumIterations` guard was dropped — restored 2026-08-19

DREAM3D 6.5.171 rejects a non-positive iteration count at preflight — `ErodeDilateBadData.cpp:141-146` returns error **-5555**, "The number of iterations (%1) must be positive". The SIMPLNX port carried the parameter across but not the check. The value flows straight into `ErodeDilateBadDataInputValues::NumIterations` and then into `for(i = 0; i < NumIterations; i++)` in `Algorithms/ErodeDilateBadData.cpp`, so `0` or any negative value makes the loop body never execute: the filter preflights clean, executes clean, reports success, and leaves every array byte-identical. A user who typed `0` got a silent no-op with no indication that the stage had done nothing.

This is a **port regression in diagnostics, not in numerics** — no output value was ever wrong, and every positive iteration count behaves identically to legacy. It was not caught by the 28-combination legacy A/B because that sweep only exercises `NumIterations ∈ {1, 2}`.

Restored 2026-08-19 as `k_InvalidNumIterationsError` = **-14603** in `ErodeDilateBadDataFilter.cpp` `preflightImpl`, guarding `NumIterations < 1` and naming the offending value in the message. Filed as `ErodeDilateBadDataFilter-D2`. The code continues this filter's `-146xx` series (`-14601`, `-14602`); the sibling `ErodeDilateMask` took `-14701` and `ErodeDilateCoordinationNumber` took `-16800`/`-16801` in their own series on the same date, per the requester's family-wide decision that the whole erode/dilate family validate `NumIterations` uniformly and match 6.5.171. No parameter key changed, so no `parametersVersion` bump. Covered by the new `Invalid Number of Iterations` TEST_CASE (coverage Path 15).

### Investigated, disproven, reverted: Dilate tie-break "fix"

A plausible-looking bug hypothesis (last-bad-neighbor-wins vs. first-bad-neighbor-wins, for a good voxel with multiple bad neighbors) was implemented as a fix and then falsified by an actual legacy binary run. Reverted in full. Recorded as a confirmed non-deviation in the deviations doc so it isn't relitigated. This is why the Oracle section emphasizes binary-verified results over source-only reasoning: source comparison alone did not catch it, since legacy's own source has the identical "unconditional overwrite" line. Only running both binaries against the same input and diffing a value that is not blind to the tie-break (`Misc`, not `FeatureIds`) surfaced the truth.

## Oracle

*Class:* **2 (Reference implementation)** — expected values are genuine DREAM3D 6.5.171 output, at two different scales.

*Applied:* Two oracles, both sourced from the legacy filter and neither derived from SIMPLNX output.

- **Small-scale (28 combinations).** `CreateTestData()` builds an in-memory 4×4×2 (32-voxel) `ImageGeom` with a hand-authored `FeatureIds` array (features 1–6; bad voxels at flat indices 0, 10, 13, 14, 31) and a `Misc` `int32` array initialized so `Misc[i] == i`, making every copied tuple traceable to its source voxel by value alone. Pipeline JSONs (`DataContainerReader` → `ErodeDilateBadData` → `DataContainerWriter`) were run through the DREAM3D 6.5.171 `PipelineRunner` against an HDF5 twin of that fixture — verified byte-for-byte identical in dims, `FeatureIds`, and `Misc` before use — for all **28 combinations** of {Dilate, Erode} × {X, XY, XYZ, XZ, Y, YZ, Z} × {1, 2 iterations}. Outputs were diffed with `h5py` against SIMPLNX: **28/28 exact matches** on both arrays. Those legacy arrays are what the `k_ExemplarFeatureIds*` / `k_ExemplarData*` constants now hold — they were regenerated from the legacy binary, not hand-traced, which removes any question of their having been fitted to SIMPLNX's own behavior.
- **Production-scale (1 combination).** `6_6_erode_dilate_test.tar.gz` carries `Exemplar Bad Data Erode` and `Exemplar Bad Data Dilate` containers generated by the legacy SIMPL `ErodeDilateBadData` filter (UUID `3adfe077-…`, Erode and Dilate, `NumIterations=2`, all three directions on) on a 189×201×20 Small IN100 slice. The archive's embedded pipeline records that legacy build as `FilterVersion 6.6.338` — later than the 6.5.171 baseline — so this exemplar is genuine legacy output but is **not itself a 6.5.171 comparison**; the 6.5.171 comparison of record is the 28-combination run above. See `## Exemplar archive` and its provenance sidecar for both caveats.

*Coverage split:* the archive exemplar was generated with all three directions enabled, so it cannot discriminate direction gating — a build with D1 still present passes it. That is exactly the gap the 28-combination sweep closes, and why both oracles were needed during this pass. Conversely the 32-voxel fixture is single-typed and single-component, so only the archive test — removed 2026-08-18, see dated note above — exercised `copyTuple` across mixed types and component counts (`EulerAngles` float32×3, `IPFColor` uint8×3, `Mask` uint8, `Phases` int32, `Confidence Index`/`Image Quality` float32).

**This half of the split was an accepted gap from 2026-08-18 to 2026-08-19; it is now partly closed.** When the archive test was retired, nothing in the suite exercised the mixed-type/multi-component `copyTuple` dispatch (Path 12), more than one concurrent transfer task (Path 13), `GenerateDataArrayList` against a heterogeneous DataStructure (Path 14), or the algorithm at production scale (759,780 cells vs. 32). The remedy then flagged as "the obvious way to close it" — a small hand-built multi-type fixture requiring no archive — was implemented on **2026-08-19** as `SimplnxCore::ErodeDilateBadDataFilter(Multi-Type Transfer)`. It closes Paths 12 and 13 outright and reclassifies Path 14 (row 14 explains why that path turned out not to be a branch at all).

What the restoration does **not** recover, and what therefore remains an accepted gap:

- **Production scale.** 32 cells against 759,780. Nothing in the suite now runs this algorithm on a realistic volume. If a defect only manifests at scale — a chunked/out-of-core access pattern, an integer-width overflow in the flat indexing, a genuine data race that a 5-task/32-voxel run is too small and too short to expose — CI will not see it. This is the substantive residual risk, and it is accepted deliberately: the retirement bought independence from a shared 68 MB archive whose retirement is itself a program goal.
- **Real-world array names and shapes.** The archive carried `EulerAngles` float32×3, `IPFColor` uint8×3, `Mask` uint8, `Phases` int32, `Confidence Index`/`Image Quality` float32. The inline fixture reproduces that *variety* of type/component shapes, not those specific arrays.

The 28-combination oracle remains broader in *parameter* space and narrower in *data-shape* space; the multi-type fixture widens the data-shape axis at toy scale without touching the scale axis.

*Encoded:*

- `SimplnxCore::ErodeDilateBadDataFilter(Erode) Expanded` and `(Dilate) Expanded` — each `GENERATE`s `dirX,dirY,dirZ ∈ {true,false}` and `numIterations ∈ {1,2}`, reports the invalid all-directions-off combination with `SUCCEED`, and looks the expected arrays up in the 28-row `k_Exemplars` table. 14 valid parameterized runs each, both `FeatureIds` and `Misc` asserted, 1039 assertions each — all pass.
- `SimplnxCore::ErodeDilateBadDataFilter(Multi-Type Transfer)` — a **Class 1 (hand-derived)** oracle, unlike the two sweeps above. The neighbor map for Erode/all-directions/1-iteration (`0←1, 10←11, 13←9, 14←15, 31←15`) is derived voxel-by-voxel from `Algorithms/ErodeDilateBadData.cpp` in the comment above `k_ErodeXYZ1SourceTuple` in the test file — traversal order `[-Z,-Y,-X,+X,+Y,+Z]`, offsets `[-16,-4,-1,+1,+4,+16]`, strict `current > most` so ties keep the first-traversed neighbor — and written into the test before the first run. It is independently corroborated by the legacy-captured `k_ExemplarDataErodeXYZ1` constants, which reduce to exactly the same five substitutions. 322 assertions; passed on first execution, so no oracle-vs-SIMPLNX adjudication was required.
- ~~`SimplnxCore::ErodeDilateBadDataFilter(Erode)` — `UnitTest::CompareExemplarToGeneratedData` against `6_6_erode_dilate_bad_data.dream3d`, 55 assertions.~~ Existed and passed in PR #1687; removed 2026-08-18, superseded by the inline 28-combination oracle; see note above.

*Second-engineer review:* **delegated to the PR reviewer** (requester decision 2026-08-19, consistent with the program plan's standing practice). The prior pass's open items — erode/dilate tie-break order, and whether direction combinations produce genuinely different output — were both resolved this pass by the legacy binary comparison above rather than by review alone. Sign-off on the oracle design is now carried by the review of the pull request that lands this work rather than by a separately named engineer.

## Code path coverage

13 of 15 paths exercised, with Path 11 the sole genuine remaining gap and Path 14 reclassified (see its row). Path 15 was added on 2026-08-19 with the restored `NumIterations` guard and is covered from the outset. The algorithm has three logical phases: (a) preflight validation, (b) a per-voxel vote/mark scan over face neighbors, and (c) a per-array transfer pass.

Paths 12–14 were exercised until 2026-08-18 by the archive-pinned `(Erode)` test and are listed separately rather than folded into rows 1/6/7, so that the coverage the retirement forfeited stays visible in the tally rather than disappearing into a row that still reads as "tested." Paths 12 and 13 were restored 2026-08-19 by the inline `(Multi-Type Transfer)` test; the rows are kept separate for the same reason, so the restoration is auditable against what was lost.

Source: `src/Plugins/SimplnxCore/src/SimplnxCore/Filters/Algorithms/ErodeDilateBadData.cpp` (231 lines).

| # | Phase | Path | Test case |
|---|-------|------|-----------|
| 1 | (a) Setup | `numFeatures` scan, face-offset/validity initialization, `adjustValidNeighbors` direction masking | All tests. Functional as of this pass — in the prior revision `adjustValidNeighbors` was dead code; it is now wired in and confirmed exercised for all 6 face directions (see below). |
| 2 | (b) Per-voxel | `featureName != 0` (good voxel) → skip | All tests (most of the 32 voxels are good) |
| 3 | (b) Per-voxel | `featureName == 0` + Dilate + neighbor `feature > 0` → `neighbors[neighborPoint] = voxelIndex` | `(Dilate) Expanded`, all 6 face directions confirmed hit |
| 4 | (b) Per-voxel | `featureName == 0` + Erode + neighbor `feature > 0` → vote accumulation, `neighbors[voxelIndex] = neighborPoint` on new max (ties keep the first-processed neighbor) | `(Erode) Expanded`, all 6 face directions confirmed hit |
| 5 | (b) Per-voxel | Erode post-vote cleanup — `featureCount[feature] = 0` for each valid neighbor of the bad voxel | `(Erode) Expanded`, all 6 face directions confirmed hit |
| 6 | (c) Transfer | `neighbor >= 0` + Erode condition (`featureName == 0 && featureIds[neighbor] > 0`) → `copyTuple` | `(Erode) Expanded` over single-component `int32` (`FeatureIds`, `Misc`); `(Multi-Type Transfer)` over five type/component shapes — the mixed-type case is row 12 |
| 7 | (c) Transfer | `neighbor >= 0` + Dilate condition (`featureName > 0 && featureIds[neighbor] == 0`) → `copyTuple` | `(Dilate) Expanded` — same single-component `int32` restriction as row 6 |
| 8 | (c) Transfer | `neighbor == -1` → skip (voxel untouched this iteration) | Both `Expanded` tests, implicitly (voxels far from bad data are unchanged in every expected array) |
| 9 | (a) Preflight | `!xDirOn && !yDirOn && !zDirOn` → error `-14601` (`k_NoDirectionsError`) | `No Direction` — asserts `invalid()` and `errors()[0].code == -14601` for both operations |
| 10 | (a) Preflight | `dims[0] == 0 \|\| dims[1] == 0 \|\| dims[2] == 0` → error `-14602` (`k_NoGeometryDimensionsError`) | `No Dimensions` — directions all **on** so the `-14601` check does not mask this path (the prior revision zeroed them and never reached here); asserts error code is exactly `-14602` |
| 11 | (b) Cancel | `m_ShouldCancel` read once per Z-slice inside the iteration loop → early return | *Not directly tested. Requires cancel-signal injection; no test sets `m_ShouldCancel` and asserts early termination.* Legacy has no cancel check at all, so SIMPLNX is ahead of legacy here — not a deviation. |
| 12 | (c) Transfer | `IDataArray::copyTuple` dispatched virtually over **non-`int32`, multi-component** arrays | **`(Multi-Type Transfer)`** — restored 2026-08-19. The fixture's cell AM carries five non-`FeatureIds` arrays spanning five distinct type/component shapes: `Misc` int32×1, `Float32x3` float32×3, `UInt8x1` uint8×1, `Float64x2` float64×2, `Int64x1` int64×1 — so five different `copyTuple` template instantiations are reached through the `IDataArray` virtual interface. Every element is index-encoded, and the per-component offsets differ within a tuple, so the assertions pin the *source tuple and component* of each copied element rather than merely observing that a value changed. Gap 2026-08-18 → 2026-08-19; toy scale only, the archive's production-scale variant stays retired. |
| 13 | (c) Transfer | **More than one concurrent** `ErodeDilateBadDataTransferDataImpl` task in the `ParallelTaskAlgorithm` (`Algorithms/ErodeDilateBadData.cpp:208-226`) | **`(Multi-Type Transfer)`** — restored 2026-08-19. Five non-`FeatureIds` arrays means five tasks enqueued on the `ParallelTaskAlgorithm` with parallelization enabled, sharing one `featureIds` store and one `neighbors` vector. This is the concurrency class the project's thread-safety guidance specifically warns about; the tasks write disjoint arrays and only read the shared state. Run three consecutive times on 2026-08-19 with no flake. The other fixtures still enqueue exactly one non-`FeatureIds` task (`Expanded`, `No Direction`) or zero (`Ignored Path`). |
| 14 | (a) Setup | `GenerateDataArrayList` over a **heterogeneous** DataStructure — feature and ensemble AttributeMatrices alongside the cell AM | **Reclassified 2026-08-19 — this is not a distinct branch.** Source review of `GenerateDataArrayList` (`src/simplnx/Utilities/DataGroupUtilities.cpp:129-174`) shows there is no tuple-count test and no AttributeMatrix-type test anywhere in it. It takes the parent group of the FeatureIds path and calls `parent.findAllChildrenOfType<IDataArray>()` with the **default `recursive == false`** (`BaseGroup.hpp:219`), then removes only paths named in `IgnoredDataArrayPaths`. Selection is purely structural: direct children of the FeatureIds' own AttributeMatrix. A sibling feature/ensemble AM is therefore never enumerated, so no filtering branch is taken and the row as originally written described a path that does not exist. `(Multi-Type Transfer)` adds a 7-tuple `CellFeatureData` AM alongside the 32-tuple cell AM and asserts its array is untouched — a **regression guard, not an exercised branch**. What it genuinely pins: if the selection rule is ever made recursive or re-rooted at the geometry, that array would join the transfer batch and `copyTuple(neighbor, i)` would be called with cell indices up to 31 against a 7-tuple array. That out-of-range hazard is real, and this assertion is what would catch it. |
| 15 | (a) Preflight | `NumIterations < 1` → error `-14603` (`k_InvalidNumIterationsError`) | **`Invalid Number of Iterations`** — added 2026-08-19 with the guard itself. Previously **unlisted**: SIMPLNX had no such branch, because the port dropped legacy's `-5555` check (see Bug Fixes) and a non-positive value silently no-opped instead. Asserts `invalid()`, `errors()[0].code == -14603`, and that the message names the offending value, for `NumIterations ∈ {0, -3}` × both operations; a positive control at `NumIterations == 1` asserts the guard does not over-trigger on the smallest legal value. |

**Per-direction coverage, confirmed by instrumentation this pass:** `Algorithms/ErodeDilateBadData.cpp` was temporarily instrumented with per-face-direction hit counters at (a) the point immediately after the `isValidFaceNeighbor` gate in the vote/mark loop, (b) the point where the Dilate mark / Erode vote condition (`feature > 0`) fires, and (c) the equivalent point in the Erode cleanup loop. Running the full `(Erode) Expanded` + `(Dilate) Expanded` sweep produced non-zero counts for **every one of the 6 directions at all 3 measurement points** — vote/mark loop reached counts `-Z=38 -Y=111 -X=108 +X=106 +Y=64 +Z=97`; Dilate marking fired at `-Z=9 -Y=46 -X=37 +X=35 +Y=16 +Z=44`; Erode voting at `-Z=8 -Y=25 -X=24 +X=24 +Y=8 +Z=32`. The instrumentation was removed afterward; this records the empirical result, not a standing code artifact.

Confirmed correct and deliberately not counted as deviations:

- **Direction masking of the Erode `featureCount` reset loop.** SIMPLNX reuses the direction-masked `isValidFaceNeighbor` in the reset loop; legacy resets over *boundary-valid* neighbors and ignores the direction flags there. Simulating both variants across all 28 combinations gives identical output, and the equivalence is general: the reset set is a superset of the increment set in both variants, so `featureCount` returns to all-zeros after every bad voxel either way.
- **`neighbors` is intentionally not reset between iterations** — matches legacy. The deviations doc records the investigation that established this.
- **`MessageHelper` shared across parallel tasks.** Each task gets its own `ThrottledMessenger` (independent timing state) over a shared `std::shared_ptr<Messenger>`; `trySendMessage` is the documented cross-thread path.

## Test inventory

| Test case | Status | Notes |
|-----------|--------|-------|
| `SimplnxCore::ErodeDilateBadDataFilter(Erode)` | retired | Retired 2026-08-18. Class 2 production-scale oracle. `6_6_erode_dilate_test.tar.gz`, Erode, all directions on, 2 iterations, 189×201×20. `CompareExemplarToGeneratedData` over the 6 cell arrays present in both containers (`Confidence Index`, `EulerAngles`, `FeatureIds`, `Image Quality`, `Mask`, `Phases`; the exemplar's `IPFColor` has no counterpart in `Input Data` and is skipped); 55 assertions. The only test that exercised `copyTuple` across mixed types and component counts, >1 concurrent transfer task, and a heterogeneous DataStructure (coverage Paths 12–14). Existed and passed in PR #1687; retired 2026-08-18 as superseded **in parameter space** by the inline 28-combination oracle, forfeiting Paths 12–14 as an accepted gap — see the dated note and *Coverage split* above. |
| `SimplnxCore::ErodeDilateBadDataFilter(Erode) Expanded` | new-for-V&V | Class 2 small-scale oracle. `GENERATE` over 7 valid direction combinations × 2 iteration counts (14 runs); all-off reported via `SUCCEED`. Both `FeatureIds` and `Misc` asserted against the `k_Exemplars` table; 1039 assertions. Modified this pass: expected arrays regenerated from legacy output (previously byte-identical across direction combinations, per D1), the `Misc` assertion enabled for the first time, and the two duplicated dispatch helpers collapsed into one table lookup. |
| `SimplnxCore::ErodeDilateBadDataFilter(Dilate) Expanded` | new-for-V&V | Same sweep and same table, Dilate operation; 1039 assertions. Same modifications as the Erode sweep. |
| `SimplnxCore::ErodeDilateBadDataFilter(Multi-Type Transfer)` | new 2026-08-19 | Class 1 hand-derived oracle. Restores coverage Paths 12–13 at toy scale after the 2026-08-18 archive-test retirement, and adds the Path 14 regression guard. Extends the 4×4×2 fixture with `Float32x3` (float32×3), `UInt8x1` (uint8×1), `Float64x2` (float64×2) and `Int64x1` (int64×1) in the cell AM alongside the existing `Misc` int32×1 — five non-`FeatureIds` arrays, hence five distinct `copyTuple` instantiations and five concurrent `ErodeDilateBadDataTransferDataImpl` tasks — plus a 7-tuple `CellFeatureData` AM asserted untouched. Runs Erode / all directions / 1 iteration, the combination whose neighbor map (`0←1, 10←11, 13←9, 14←15, 31←15`) is derived by hand from the algorithm source in the test's comment block and corroborated by the file's legacy-captured `k_ExemplarDataErodeXYZ1`. Per-element assertions with `CAPTURE(i)` on all five cell arrays plus the feature array; `FeatureIds` and `Misc` reuse the existing `CheckOutput` table lookup. 322 assertions. Also strengthens the FeatureIds-last ordering pin: every expectation depends on the transfer stage reading **pre-update** `FeatureIds`, so hoisting the `FeatureIds` update ahead of the parallel batch fails the test. |
| `SimplnxCore::ErodeDilateBadDataFilter Ignored Path` | new-for-V&V | Confirms an array listed in `IgnoredDataArrayPaths` (`Misc`) is left untouched, for both operations via `GENERATE(k_Dilate, k_Erode)`; 91 assertions. Modified this pass: the prior revision only preflighted, so comparing the DataStructure against a fresh fixture could not fail. It now executes the filter and additionally asserts that `FeatureIds` *did* change, which is what keeps the ignored-path check from passing vacuously. Verified by mutation — emptying the ignore list, or dropping the `execute()` call, each now fails the test. |
| `SimplnxCore::ErodeDilateBadDataFilter No Direction` | new-for-V&V | Preflight-error test: all directions off, geometry otherwise valid, both operations × 2 iteration counts. Asserts `invalid()` and `errors()[0].code == -14601`; 25 assertions. Covers Path 9. |
| `SimplnxCore::ErodeDilateBadDataFilter No Dimensions` | new-for-V&V | Preflight-error test: `ImageGeom` dimensions forced to `{0,0,0}`, directions all on, Dilate. Asserts `invalid()` and `errors()[0].code == -14602`; 7 assertions. Covers Path 10. Modified this pass: previously it also zeroed all direction flags, which tripped `-14601` first and left Path 10 unreached. |
| `SimplnxCore::ErodeDilateBadDataFilter Invalid Number of Iterations` | new 2026-08-19 | Preflight-error test written **before** the guard it covers (TDD): it failed on `invalid()` for all four negative cases against the pre-guard build, then passed once `k_InvalidNumIterationsError` was added. Two SECTIONs — a negative section over `NumIterations ∈ {0, -3}` × `GENERATE(k_Dilate, k_Erode)` asserting `invalid()`, `errors()[0].code == -14603`, and that the message text contains the offending value; and a positive control at `NumIterations == 1` asserting the smallest legal value still preflights `VALID`, so the guard cannot be satisfied by an over-broad `<= 1` comparison. Directions all on and geometry valid, so neither `-14601` nor `-14602` masks the new path. 39 assertions. Covers Path 15. |
| `SimplnxCore::ErodeDilateBadDataFilter: SIMPL Backwards Compatibility` | kept | `DYNAMIC_SECTION` over `simpl_conversion/6_5/ErodeDilateBadDataFilter.json` (matched by `Filter_Uuid`) and `simpl_conversion/6_4/ErodeDilateBadDataFilter.json` (matched by `Filter_Name`; that fixture has no UUID field). Loads each legacy pipeline via `Pipeline::FromSIMPLFile`, confirms a single `PipelineFilter` with the right UUID, and checks the converted arguments: `Operation == k_Dilate`, `NumIterations == 5`, `XDirOn/YDirOn/ZDirOn == true`, geometry path `DataPath({"DataContainer"})`, feature-ids path `DataPath({"DataContainer","CellData","TestArray"})`; 27 assertions. `IgnoredDataArrayPaths` verified only by successful pipeline load, matching the pattern in `FillBadDataTest.cpp`. Not an oracle test. |

All 8 tests pass in the in-core build (`NX-Com-Qt69-Vtk96-Rel`): **measured 2026-08-19, post-guard — 8/8 passed, 2589 assertions** (1039 + 1039 + 91 + 25 + 7 + 39 + 322 + 27), each figure read off the `ctest --verbose` run itself rather than computed. `ctest -R "SimplnxCore::ErodeDilateBadData" --verbose` was run **three consecutive times** on that date because `(Multi-Type Transfer)` is the only test in the suite that runs more than one transfer task concurrently; all three runs were identical, 8/8, no flake. Had it flaked, that would have been reported as a genuine algorithm thread-safety finding rather than worked around.

The out-of-core build (`simplnx-ooc-Rel`) has **not** been re-run since the `(Erode)` retirement, and that re-run is **waived** (requester decision 2026-08-19) — it is no longer an item blocking promotion to COMPLETE. The standing OOC result — all tests passing with assertion counts identical to in-core — is from the **pre-removal** run of 7 tests (2283 assertions) and predates the retirement, the `(Multi-Type Transfer)` addition, and the `NumIterations` guard.

## Exemplar archive

- **Archive:** `6_6_erode_dilate_test.tar.gz`
- **SHA512:** `5f0773e5d296936effbb2239965f5847e7c18533b0a2c3ec6a1d6a83b03417e5b459cce29808c8e0273613b3b6fa032c675e84926eb35d8da8a6ddc0641a0ef5`
- **Provenance:** `src/Plugins/SimplnxCore/vv/provenance/6_6_erode_dilate_test.md`

The archive is shared: `6_6_erode_dilate_bad_data.dream3d` serves this filter, `6_6_erode_dilate_mask.dream3d` serves `ErodeDilateMaskTest`, and `6_6_erode_dilate_coordination_number.dream3d` serves `ErodeDilateCoordinationNumberTest`. Any regeneration must account for all three consumers.

## Deviations from DREAM3D 6.5.171

No confirmed legacy deviations. The comparison was run at both scales described in the Oracle section — 28/28 exact matches on the 32-voxel fixture, and an element-wise match against the legacy-generated exemplar on the 189×201×20 Small IN100 slice.

Two SIMPLNX-side port omissions were found and fixed:

- `ErodeDilateBadDataFilter-D1` — the `XDirOn`/`YDirOn`/`ZDirOn` parameters had no effect on which face neighbors participated — see [`deviations/ErodeDilateBadDataFilter.md`](deviations/ErodeDilateBadDataFilter.md)
- `ErodeDilateBadDataFilter-D2` — a non-positive `NumIterations` was accepted and silently no-opped, where legacy rejects it at preflight with `-5555`. Restored 2026-08-19 as `-14603` — see [`deviations/ErodeDilateBadDataFilter.md`](deviations/ErodeDilateBadDataFilter.md). Numerics were never affected for any legal value; it is filed as a numbered entry rather than a prose note because it was a user-visible difference across every SIMPLNX release since the port and because restoring the guard flips previously "successful" pipelines to preflight failures, which is migration content.

One hypothesis was investigated and disproven (Dilate tie-break order: legacy matches SIMPLNX's original last-write-wins behavior), and is recorded in the same file as a confirmed non-deviation so it is not relitigated.
